import 'package:flutter/material.dart';

void main() {
  runApp(DarkGameApp());
}

class DarkGameApp extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // // TODO: implement build
    // throw UnimplementedError();
    return const MaterialApp(home: Text('Hello!!'), );
  }
}
